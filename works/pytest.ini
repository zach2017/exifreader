[pytest]
testpaths = tests
pythonpath = api-server lambda
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: Tests requiring Postgres (deselect with -m "not integration")
